services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-anco}
      POSTGRES_USER: ${POSTGRES_USER:-anco}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-anco-dev}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-anco} -d ${POSTGRES_DB:-anco}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

  cache:
    image: redis:7-alpine
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

  web:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    restart: unless-stopped
    # Em dev (DJANGO_SETTINGS_MODULE=config.settings.dev), usar runserver:
    #   command: python manage.py runserver 0.0.0.0:8000
    # Em prod (settings.prod), gunicorn:
    command: >
      gunicorn config.wsgi:application
      --bind 0.0.0.0:8000
      --workers 3
      --timeout 60
      --access-logfile -
      --error-logfile -
    env_file:
      - ../.env
    ports:
      # Bind apenas no loopback do host: nginx faz reverse proxy
      - "127.0.0.1:9090:8000"
    volumes:
      - ..:/app
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_healthy
    networks:
      - internal

  worker:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    restart: unless-stopped
    command: python manage.py qcluster
    env_file:
      - ../.env
    volumes:
      - ..:/app
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_healthy
    networks:
      - internal
    profiles:
      - worker  # opcional em dev (tasks rodam sync); ligar com --profile worker

  embeddings:
    build:
      context: ./embeddings
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      EMBEDDINGS_MODEL: ${EMBEDDINGS_MODEL:-paraphrase-multilingual-MiniLM-L12-v2}
    volumes:
      - model_cache:/model_cache
    healthcheck:
      # python:3.12-slim não tem curl; usa urllib (sempre disponível)
      test: ["CMD-SHELL", "python -c 'import urllib.request,sys; r=urllib.request.urlopen(\"http://localhost:8080/health\",timeout=3).read(); sys.exit(0 if b\"\\\"ready\\\":true\" in r else 1)'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - internal
    profiles:
      - embeddings  # sobe com --profile embeddings (ou prod quando configurado)

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      ANCO_DOMAIN: ${ANCO_DOMAIN:-anco.paulovicente.pro.br}
      ANCO_ADMIN_EMAIL: ${ANCO_ADMIN_EMAIL:-paulovicente.ifba@gmail.com}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ../staticfiles:/srv/static:ro
      - ../media:/srv/media:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
    networks:
      - internal
    profiles:
      - prod  # so sobe em producao (`--profile prod`); dev usa :8000 direto

volumes:
  pgdata:
  caddy_data:
  caddy_config:
  model_cache:

networks:
  internal:
