# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Virtualenv
.venv/
venv/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Environment
.env
.env.local
.env.*.local

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Lint / format caches
.ruff_cache/
.mypy_cache/

# Editors
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Docker volumes (caso bind-mounts ad-hoc)
pgdata/

# CSS gerado pelo Tailwind (reconstruído no Docker build e em dev com --watch)
static/css/output.css

# Artefatos gerados pelas ferramentas de recuperação de DOI / migração (tools/)
/dois_recuperados.json
/dois_verificados.json
/dois_verificados_test.json
/conflitos_doi.csv

# Exports da base (dump de dados, padrão base-anco-revisada.json)
/base-anco-export-*.json

# Auditorias/correções de DOI em produção e backups (dados, não versionar)
/prod_doi_*.csv
/backup_triagem_pre_delete.json

# Backups e dumps de dados (nunca versionar: contêm dados de usuários da plataforma)
/backups/
/docs_backup_*.tar.gz
/proposta_resumos_legado.csv
