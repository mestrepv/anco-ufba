from django.apps import AppConfig


class VocabularioConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.vocabulario"
    label = "vocabulario"
    verbose_name = "Vocabulários controlados"
